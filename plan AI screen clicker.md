
I would like to create a new feature where we have a voice command that says 'identify ten of spades' for example  and the current application screen is used and sent to openai to identify what part of the screen it's most likely needing to be clicked on if there are more than one elements that match the voice command this should show a label overlay with corresponding numbers on the screen for the user to select which element they want to interact with.

We need to identify which OpenAI API to use  and also we need to estimate the cost of this in case it's too expensive to use on a regular basis.

If it is too expensive we may have to look into using OCR instead and investigate if this is possible to do in real-time with the current technology available.