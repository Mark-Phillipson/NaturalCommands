
 can we build a special mode for the show labels where it shows them as normal but then when a label is activated it redraws them as the screen changes and waits for the next one to be activated and only pressing escape we'll stop them coming back each time?
 